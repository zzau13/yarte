<html>
<head>
    <title>{{ year }}</title>
</head>
<body>
<h1>CSL {{ year }}</h1>
<ul>
    {{~#each teams ~}}
        <li class="{{#if first }}champion{{/if}}">
            <b>{{ this.name }}</b>: {{ this.score ~}}
        </li>
    {{~/each ~}}
</ul>
</body>
</html>
